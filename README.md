# cssMemeSlider

git commit -m "init:... `LANG=en_EN date +'(%a, %b %d, %Y %r)'`"
git commit -m "feat:  `LANG=en_EN date +'(%a, %b %d, %Y %r)'`"
git commit -m "fix:  `LANG=en_EN date +'(%a, %b %d, %Y %r)'`"
